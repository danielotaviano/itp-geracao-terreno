#include <stdio.h>

int geracao_terreno(void) {
  
  int imageWidth, imageHeight, roughness;

  imageWidth = 513;

  int heights[imageWidth];

  for (int n = 0; n < imageWidth; n++) {
    heights[n] = 0;
  }

  return 0;
}